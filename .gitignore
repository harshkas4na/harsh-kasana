node_modules
.next
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# not referenced anywhere in the app — local only
my-videos/
# Next/TS build artifact — regenerated on every build, never worth a diff
tsconfig.tsbuildinfo
